---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-deployment
  labels:
    app: server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: server
  template:
    metadata:
      labels:
        app: server
    spec:
      volumes:
        - name: modelpd
          nfs:
            server: 10.10.3.80
            path: /var/nfs/public
      containers:
        - name: server
          image: lazar010/ruo
          imagePullPolicy: Always
          resources:
            limits:
              ephemeral-storage: 5Gi
            requests:
              ephemeral-storage: 2Gi
          ports:
            - containerPort: 8000
          env:
            - name: MODEL_PATH
              value: /app/model
          volumeMounts:
            - mountPath: /app/model
              name: modelpd
      

---
apiVersion: v1
kind: Service
metadata:
  name: server-service
spec:
  selector:
    app: server
  type: LoadBalancer
  externalIPs:
  - 10.10.3.81
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30001